{
  "build": [
    [
      "#{os == 'windows' ? 'x86_64-w64-mingw32-gcc' : 'cc'}",
      "-c",
      "${CPPFLAGS}",
      "./test.c"
    ]
  ],
  "dependencies": {
    "esy-gmp": "*",
    "@opam/conf-gmp": "*"
  }
}
