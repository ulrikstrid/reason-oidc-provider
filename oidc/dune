(library
  (name oidc)
  (flags (-w -40 -w +26))
  (libraries
    lwt
    logs
    
    uri

    yojson
    str
    containers
    nocrypto
    jwt
  ))

(env
  (dev (flags (:standard -g)))
  (docker (flags (:standard -ccopt -static))))
